Creates a fingerprint and calls KeyDB REST API and Inserts the fingerprint corresponding to the SPOCID.

Also has all modules associated with general authentications e.g. interaction with eKYCSim etc.
